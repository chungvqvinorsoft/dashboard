export interface UserInfo {
    username: string,
    password: string,
}

export interface ResponeLogin {
    username: string,
    createdAt: string,
    id: string,
    password: string,
    name: string
}